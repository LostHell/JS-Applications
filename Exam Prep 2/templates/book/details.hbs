{{>header}}
<div class="container details">
    <div class="details-content">
        <h2>{{myBook.title}}</h2>
        <strong>{{myBook.category}}</strong>
        <p>{{myBook.content}}</p>
        <div class="buttons">
            <a href="#/delete/{{myBook.objectId}}" class="btn delete">Delete</a>
            <a href="#/edit/{{myBook.objectId}}" class="btn edit">Edit</a>
            <a href="#/" class="btn edit">Back</a>
        </div>
    </div>
</div>
{{>footer}}